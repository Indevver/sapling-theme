{% extends 'layouts/base.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ title }}</h1>
                {# @TODO as search form #}
            </div>
        </div>
    </div>
{% endblock %}