{% extends 'layouts/base.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ title ?? post.title }}</h1>
                {{ content ?? post.content }}
            </div>
        </div>
    </div>
{% endblock %}