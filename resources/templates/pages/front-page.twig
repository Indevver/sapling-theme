{% extends 'layouts/base.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{ post.title }}</h1>
                {{ post.content }}
            </div>
        </div>
    </div>
{% endblock %}