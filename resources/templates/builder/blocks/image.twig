{% extends 'builder/blocks/base.twig' %}
{% block block_content %}
    <img src="{{ Image(item.Image).src }}" alt="{{ Image(item.Image).alt }}" title="{{ Image(item.Image).title }}" class="img-fluid"/>
{% endblock %}