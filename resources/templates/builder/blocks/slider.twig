{% extends 'builder/blocks/base.twig' %}
    {% block block_content %}
    {% for image in item.Slides %}
        <img src="{{ Image(image.Slide).src }}" alt="{{ Image(image.Slide).alt }}" title="{{ Image(image.Slide).title }}"/>
    {% endfor %}
{% endblock %}